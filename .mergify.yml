commands_restrictions:
  queue:
    conditions:
      - sender = {{ author }}
queue_rules:
  - name: vm3-dynamic-author-identity
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = vm3-dynamic-author-never-260920
    merge_method: merge
    batch_size: 1
merge_queue:
  mode: serial
  max_parallel_checks: 1
